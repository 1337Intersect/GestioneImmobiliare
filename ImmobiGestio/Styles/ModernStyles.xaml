<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ENHANCED PALETTE COLORI MODERNA -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="#2196F3"/>
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#1976D2"/>
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="#E3F2FD"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#FF4081"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
    <SolidColorBrush x:Key="SuccessLightBrush" Color="#E8F5E8"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#FF9800"/>
    <SolidColorBrush x:Key="WarningLightBrush" Color="#FFF3E0"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#F44336"/>
    <SolidColorBrush x:Key="PurpleBrush" Color="#9C27B0"/>
    <SolidColorBrush x:Key="PurpleLightBrush" Color="#F3E5F5"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#212121"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#757575"/>
    <SolidColorBrush x:Key="TextMutedBrush" Color="#999999"/>
    <SolidColorBrush x:Key="DividerBrush" Color="#E0E0E0"/>

    <!-- ENHANCED SHADOWS -->
    <DropShadowEffect x:Key="CardShadow"
                      Color="#000000"
                      Opacity="0.08"
                      ShadowDepth="4"
                      BlurRadius="16"/>

    <DropShadowEffect x:Key="CardShadowHover"
                      Color="#000000"
                      Opacity="0.15"
                      ShadowDepth="8"
                      BlurRadius="24"/>

    <DropShadowEffect x:Key="ButtonShadow"
                      Color="#000000"
                      Opacity="0.1"
                      ShadowDepth="2"
                      BlurRadius="8"/>

    <!-- ANIMATIONS -->
    <Storyboard x:Key="HoverScaleAnimation">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                         To="1.02" Duration="0:0:0.2">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                         To="1.02" Duration="0:0:0.2">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="HoverScaleBackAnimation">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                         To="1.0" Duration="0:0:0.2"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                         To="1.0" Duration="0:0:0.2"/>
    </Storyboard>

    <Storyboard x:Key="FadeInAnimation">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.5"/>
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                         From="20" To="0" Duration="0:0:0.5">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- ENHANCED STAT CARD STYLES -->
    <Style x:Key="EnhancedStatCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="24,20"/>
        <Setter Property="Margin" Value="12,8"/>
        <Setter Property="BorderThickness" Value="0,0,0,4"/>
        <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Trigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource HoverScaleAnimation}"/>
                </Trigger.EnterActions>
                <Trigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource HoverScaleBackAnimation}"/>
                </Trigger.ExitActions>
                <Setter Property="Effect" Value="{StaticResource CardShadowHover}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- STAT CARD VARIANTS -->
    <Style x:Key="StatCardProperties" TargetType="Border" BasedOn="{StaticResource EnhancedStatCardStyle}">
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
    </Style>

    <Style x:Key="StatCardClients" TargetType="Border" BasedOn="{StaticResource EnhancedStatCardStyle}">
        <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}"/>
    </Style>

    <Style x:Key="StatCardAppointments" TargetType="Border" BasedOn="{StaticResource EnhancedStatCardStyle}">
        <Setter Property="BorderBrush" Value="{StaticResource WarningBrush}"/>
    </Style>

    <Style x:Key="StatCardRevenue" TargetType="Border" BasedOn="{StaticResource EnhancedStatCardStyle}">
        <Setter Property="BorderBrush" Value="{StaticResource PurpleBrush}"/>
    </Style>

    <!-- ICON CONTAINERS -->
    <Style x:Key="StatIconContainer" TargetType="Border">
        <Setter Property="Width" Value="48"/>
        <Setter Property="Height" Value="48"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
    </Style>

    <Style x:Key="StatIconContainerProperties" TargetType="Border" BasedOn="{StaticResource StatIconContainer}">
        <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
    </Style>

    <Style x:Key="StatIconContainerClients" TargetType="Border" BasedOn="{StaticResource StatIconContainer}">
        <Setter Property="Background" Value="{StaticResource SuccessLightBrush}"/>
    </Style>

    <Style x:Key="StatIconContainerAppointments" TargetType="Border" BasedOn="{StaticResource StatIconContainer}">
        <Setter Property="Background" Value="{StaticResource WarningLightBrush}"/>
    </Style>

    <Style x:Key="StatIconContainerRevenue" TargetType="Border" BasedOn="{StaticResource StatIconContainer}">
        <Setter Property="Background" Value="{StaticResource PurpleLightBrush}"/>
    </Style>

    <!-- ICON STYLES -->
    <Style x:Key="StatIcon" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="StatIconProperties" TargetType="TextBlock" BasedOn="{StaticResource StatIcon}">
        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Text" Value="&#xE80F;"/>
        <!-- Home icon -->
    </Style>

    <Style x:Key="StatIconClients" TargetType="TextBlock" BasedOn="{StaticResource StatIcon}">
        <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
        <Setter Property="Text" Value="&#xE716;"/>
        <!-- People icon -->
    </Style>

    <Style x:Key="StatIconAppointments" TargetType="TextBlock" BasedOn="{StaticResource StatIcon}">
        <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
        <Setter Property="Text" Value="&#xE787;"/>
        <!-- Calendar icon -->
    </Style>

    <Style x:Key="StatIconRevenue" TargetType="TextBlock" BasedOn="{StaticResource StatIcon}">
        <Setter Property="Foreground" Value="{StaticResource PurpleBrush}"/>
        <Setter Property="Text" Value="&#xE1D0;"/>
        <!-- Money icon -->
    </Style>

    <!-- TYPOGRAPHY STYLES -->
    <Style x:Key="StatNumberStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="32"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,0,0,4"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
    </Style>

    <Style x:Key="StatLabelStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <Style x:Key="StatSubtitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
        <Setter Property="Margin" Value="0,4,0,0"/>
    </Style>

    <!-- ENHANCED BUTTONS -->
    <Style x:Key="ModernButtonPrimary" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Effect" Value="{StaticResource ButtonShadow}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- SIDEBAR STYLES -->
    <Style x:Key="SidebarButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                        VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F5F5F5"/>
                            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ENHANCED CARD STYLE -->
    <Style x:Key="ContentCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="Padding" Value="24"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
    </Style>

    <!-- STATUS BADGES -->
    <Style x:Key="StatusBadge" TargetType="Border">
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="StatusBadgeAvailable" TargetType="Border" BasedOn="{StaticResource StatusBadge}">
        <Setter Property="Background" Value="{StaticResource SuccessLightBrush}"/>
    </Style>

    <Style x:Key="StatusBadgeSold" TargetType="Border" BasedOn="{StaticResource StatusBadge}">
        <Setter Property="Background" Value="{StaticResource PurpleLightBrush}"/>
    </Style>

    <Style x:Key="StatusTextAvailable" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Text" Value="DISPONIBILE"/>
    </Style>

    <Style x:Key="StatusTextSold" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource PurpleBrush}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Text" Value="VENDUTO"/>
    </Style>

    <!-- ANIMATED CONTENT CONTROL -->
    <Style x:Key="AnimatedContentControl" TargetType="ContentControl">
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform Y="0"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard Storyboard="{StaticResource FadeInAnimation}"/>
            </EventTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>