<UserControl x:Class="ImmobiGestio.Views.AppuntamentiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="#FF2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#FFCCCCCC"/>
                    <Setter Property="Foreground" Value="#FF666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#FF2196F3"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Margin" Value="5,5,5,0"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#FFF5F5F5" Margin="5">
            <Button Content="📅 Nuovo Appuntamento" Command="{Binding AddAppuntamentoCommand}" 
                    Background="#FF4CAF50" FontWeight="Bold"/>
            <Button Content="💾 Salva" Command="{Binding SaveAppuntamentoCommand}" 
                    Background="#FFFF9800"/>
            <Button Content="🗑️ Elimina" Command="{Binding DeleteAppuntamentoCommand}" 
                    Background="#FFF44336"/>
            <Separator Margin="10,0"/>
            <Button Content="✅ Conferma" Command="{Binding ConfermaAppuntamentoCommand}" 
                    Background="#FF4CAF50"/>
            <Button Content="✔️ Completa" Command="{Binding CompletaAppuntamentoCommand}" 
                    Background="#FF607D8B"/>
            <Separator Margin="10,0"/>
            <Label Content="🔍" VerticalAlignment="Center" Foreground="#FF666666"/>
            <TextBox x:Name="SearchBox" Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" Margin="5,0"
                     ToolTip="Cerca appuntamenti"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Lista Appuntamenti -->
            <GroupBox Grid.Column="0" Header="📅 Lista Appuntamenti" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListBox Grid.Row="0" ItemsSource="{Binding Appuntamenti}" SelectedItem="{Binding SelectedAppuntamento}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#FFEEEEEE" BorderThickness="0,0,0,1" Padding="5">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Titolo}" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Text="{Binding DataInizio, StringFormat=dd/MM/yyyy HH:mm}" FontSize="11" Foreground="Gray"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,2">
                                            <TextBlock Text="{Binding StatoAppuntamento}" FontWeight="Bold" Foreground="Blue"/>
                                            <TextBlock Text=" • " Foreground="Gray"/>
                                            <TextBlock Text="{Binding TipoAppuntamento}" FontSize="10" Foreground="Green"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Cliente.NomeCompleto}" FontSize="10" Foreground="Gray"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <TextBlock Grid.Row="1" Text="{Binding Appuntamenti.Count, StringFormat=Totale: {0} appuntamenti}" 
                               Margin="5" FontSize="10" Foreground="Gray" HorizontalAlignment="Right"/>
                </Grid>
            </GroupBox>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="LightGray"/>

            <!-- Dettagli Appuntamento -->
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="IsEnabled" Value="True"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedAppuntamento}" Value="{x:Null}">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <!-- Placeholder quando nessun appuntamento è selezionato -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedAppuntamento}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="📅" FontSize="48" HorizontalAlignment="Center" Foreground="#FFCCCCCC"/>
                        <TextBlock Text="Seleziona un appuntamento dalla lista" FontSize="16" HorizontalAlignment="Center" 
                                   Foreground="#FF666666" Margin="0,10"/>
                        <TextBlock Text="oppure crea un nuovo appuntamento" FontSize="12" HorizontalAlignment="Center" 
                                   Foreground="#FF999999"/>
                    </StackPanel>

                    <!-- Content when appuntamento is selected -->
                    <StackPanel DataContext="{Binding SelectedAppuntamento}">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <!-- Informazioni Appuntamento -->
                        <GroupBox Header="📅 Dettagli Appuntamento" Margin="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Grid.Column="0" Content="Titolo:"/>
                                <TextBox Grid.Row="0" Grid.Column="1" 
                                         Text="{Binding Titolo, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>

                                <Label Grid.Row="1" Grid.Column="0" Content="Data Inizio:"/>
                                <DatePicker Grid.Row="1" Grid.Column="1" 
                                            SelectedDate="{Binding DataInizio, Mode=TwoWay}"/>

                                <Label Grid.Row="2" Grid.Column="0" Content="Data Fine:"/>
                                <DatePicker Grid.Row="2" Grid.Column="1" 
                                            SelectedDate="{Binding DataFine, Mode=TwoWay}"/>

                                <Label Grid.Row="3" Grid.Column="0" Content="Luogo:"/>
                                <TextBox Grid.Row="3" Grid.Column="1" 
                                         Text="{Binding Luogo, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>

                                <Label Grid.Row="4" Grid.Column="0" Content="Tipo:"/>
                                <ComboBox Grid.Row="4" Grid.Column="1" 
                                          ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.TipiAppuntamento}" 
                                          SelectedItem="{Binding TipoAppuntamento, Mode=TwoWay}"/>

                                <Label Grid.Row="5" Grid.Column="0" Content="Stato:"/>
                                <ComboBox Grid.Row="5" Grid.Column="1" 
                                          ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.StatiAppuntamento}" 
                                          SelectedItem="{Binding StatoAppuntamento, Mode=TwoWay}"/>
                            </Grid>
                        </GroupBox>

                        <!-- Cliente e Immobile -->
                        <GroupBox Header="👤 Cliente e Immobile" Margin="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Grid.Column="0" Content="Cliente:"/>
                                <ComboBox Grid.Row="0" Grid.Column="1" 
                                          ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.ClientiDisponibili}" 
                                          SelectedItem="{Binding Cliente, Mode=TwoWay}"
                                          DisplayMemberPath="NomeCompleto"/>

                                <Label Grid.Row="1" Grid.Column="0" Content="Immobile:"/>
                                <ComboBox Grid.Row="1" Grid.Column="1" 
                                          ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.ImmobiliDisponibili}" 
                                          SelectedItem="{Binding Immobile, Mode=TwoWay}"
                                          DisplayMemberPath="Titolo"/>
                            </Grid>
                        </GroupBox>

                        <!-- Descrizione e Note -->
                        <GroupBox Header="📝 Descrizione e Note" Margin="5">
                            <StackPanel>
                                <Label Content="Descrizione:"/>
                                <TextBox Text="{Binding Descrizione, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                         Height="60" TextWrapping="Wrap" AcceptsReturn="True" 
                                         VerticalScrollBarVisibility="Auto" Margin="5"/>

                                <Label Content="Note Private:"/>
                                <TextBox Text="{Binding NotePrivate, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                                         Height="40" TextWrapping="Wrap" AcceptsReturn="True" 
                                         VerticalScrollBarVisibility="Auto" Margin="5"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Stato e Priorità -->
                        <GroupBox Header="⚡ Gestione Stato" Margin="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="5">
                                    <Label Content="Priorità:"/>
                                    <ComboBox ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.PrioritaAppuntamento}" 
                                              SelectedItem="{Binding Priorita, Mode=TwoWay}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="5">
                                    <Label Content="Durata:"/>
                                    <TextBlock Text="{Binding DurataFormattata, Mode=OneWay}" 
                                               FontSize="14" 
                                               Margin="5" 
                                               Foreground="Blue"/>
                                </StackPanel>
                            </Grid>
                        </GroupBox>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>