<!-- AppuntamentiView.xaml Ottimizzato per Performance -->
<UserControl x:Class="ImmobiGestio.Views.AppuntamentiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1400"
             Background="{StaticResource BackgroundBrush}">

    <UserControl.Resources>
        <!-- ✅ STILI CENTRALIZZATI E OTTIMIZZATI -->

        <!-- Base Button Style -->
        <Style x:Key="BaseToolbarButton" TargetType="Button" BasedOn="{StaticResource OutlookToolbarButton}">
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Primary Action Button -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseToolbarButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Mini Calendar Day Style - SEMPLIFICATO -->
        <Style x:Key="MiniCalendarDayStyle" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="14"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Calendar Cell Style - OTTIMIZZATO -->
        <Style x:Key="CalendarCellStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="MinHeight" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Event Item Style - SEMPLIFICATO -->
        <Style x:Key="EventItemStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ✅ DATA TEMPLATE OTTIMIZZATI -->

        <!-- Mini Calendar Day Template -->
        <DataTemplate x:Key="MiniCalendarDayTemplate">
            <Button Content="{Binding Day}"
                    Style="{StaticResource MiniCalendarDayStyle}"
                    Command="{Binding DataContext.SelectDateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding Date}">
                <Button.Foreground>
                    <SolidColorBrush Color="{Binding ForegroundColor}"/>
                </Button.Foreground>
                <Button.Background>
                    <SolidColorBrush Color="{Binding BackgroundColor}"/>
                </Button.Background>
            </Button>
        </DataTemplate>

        <!-- Simplified Event Template -->
        <DataTemplate x:Key="SimpleEventTemplate">
            <Border Style="{StaticResource EventItemStyle}">
                <Border.Background>
                    <SolidColorBrush Color="{Binding StatoColore, Converter={StaticResource HexToColorConverter}}"/>
                </Border.Background>
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick" 
                                  Command="{Binding DataContext.SelectAppuntamentoInCalendarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  CommandParameter="{Binding}"/>
                </Border.InputBindings>
                <TextBlock Text="{Binding Titolo}" 
                           Foreground="White" 
                           FontSize="9" 
                           FontWeight="SemiBold"
                           TextTrimming="CharacterEllipsis"/>
            </Border>
        </DataTemplate>

        <!-- Today's Agenda Item Template -->
        <DataTemplate x:Key="TodayAgendaTemplate">
            <Border Style="{StaticResource EventItemStyle}" Margin="0,0,0,4" Padding="8,4">
                <Border.Background>
                    <SolidColorBrush Color="{Binding StatoColore, Converter={StaticResource HexToColorConverter}}"/>
                </Border.Background>
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftDoubleClick" 
                                  Command="{Binding DataContext.SelectAppuntamentoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                  CommandParameter="{Binding}"/>
                </Border.InputBindings>
                <StackPanel>
                    <TextBlock Text="{Binding Titolo}" 
                               Foreground="White" 
                               FontSize="11" 
                               FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding DataInizio, StringFormat=HH:mm}" 
                               Foreground="White" 
                               FontSize="10"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ✅ TOOLBAR SEMPLIFICATO -->
        <Border Grid.Row="0" 
                Background="{StaticResource SurfaceBrush}" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="0,0,0,1" 
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Main Actions -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="📅 Nuovo" 
                            Command="{Binding AddAppuntamentoCommand}"
                            Style="{StaticResource PrimaryButton}"/>

                    <Rectangle Width="1" Height="30" Fill="{StaticResource BorderBrush}" Margin="8,0"/>

                    <Button Content="💾 Salva" 
                            Command="{Binding SaveAppuntamentoCommand}"
                            Style="{StaticResource BaseToolbarButton}"/>

                    <Button Content="🗑️ Elimina" 
                            Command="{Binding DeleteAppuntamentoCommand}"
                            Style="{StaticResource BaseToolbarButton}"/>

                    <Button Content="🔄 Aggiorna" 
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource BaseToolbarButton}"/>
                </StackPanel>

                <!-- Search and Filters -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <ComboBox Width="120" 
                              SelectedItem="{Binding FiltroTipo}"
                              ItemsSource="{Binding TipiAppuntamentoFiltro}"
                              Style="{StaticResource MinimalComboBox}"
                              Margin="0,0,8,0"/>

                    <TextBox Width="200" 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MinimalTextBox}"
                             ToolTip="Cerca eventi..."/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ✅ CALENDAR NAVIGATION SEMPLIFICATO -->
        <Border Grid.Row="1" 
                Background="{StaticResource HoverBrush}" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="0,0,0,1"
                Height="50">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                    <Button Content="◀" 
                            Command="{Binding PreviousPeriodCommand}"
                            Style="{StaticResource BaseToolbarButton}"
                            Width="36" Height="36"/>
                    <Button Content="▶" 
                            Command="{Binding NextPeriodCommand}"
                            Style="{StaticResource BaseToolbarButton}"
                            Width="36" Height="36"/>
                </StackPanel>

                <TextBlock Grid.Column="1" 
                           Text="{Binding TitoloVista}"
                           FontSize="18"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Foreground="{StaticResource TextPrimary}"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                    <Button Content="Oggi" 
                            Command="{Binding TodayCommand}"
                            Style="{StaticResource PrimaryButton}"/>

                    <ComboBox Width="100"
                              SelectedItem="{Binding VistaCalendario}"
                              Style="{StaticResource MinimalComboBox}"
                              Margin="8,0,0,0">
                        <ComboBoxItem Content="Giorno"/>
                        <ComboBoxItem Content="Settimana"/>
                        <ComboBoxItem Content="Mese" IsSelected="True"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ✅ MAIN CONTENT AREA OTTIMIZZATO -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- ✅ LEFT SIDEBAR OTTIMIZZATO -->
            <Border Grid.Column="0" 
                    Background="{StaticResource SurfaceBrush}" 
                    BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="0,0,1,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16">

                        <!-- Mini Calendar Header -->
                        <TextBlock Text="{Binding SelectedDate, StringFormat='MMMM yyyy'}" 
                                   FontSize="14" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimary}" 
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,12"/>

                        <!-- ✅ MINI CALENDAR - VIRTUALIZZATO -->
                        <ItemsControl x:Name="MiniCalendarDays"
                                      ItemsSource="{Binding MiniCalendarDays}"
                                      ItemTemplate="{StaticResource MiniCalendarDayTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="7" Rows="6"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>

                        <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,20,0,16"/>

                        <!-- ✅ TODAY'S AGENDA - OTTIMIZZATO -->
                        <TextBlock Text="Agenda di oggi" 
                                   FontSize="14" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimary}" 
                                   Margin="0,0,0,12"/>

                        <!-- ✅ VIRTUALIZING STACK PANEL per Performance -->
                        <ItemsControl ItemsSource="{Binding AppuntamentiOggiCollection}"
                                      ItemTemplate="{StaticResource TodayAgendaTemplate}"
                                      MaxHeight="300">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>

                        <!-- Empty State -->
                        <TextBlock Text="Nessun appuntamento oggi" 
                                   FontSize="11" 
                                   Foreground="{StaticResource TextMuted}" 
                                   FontStyle="Italic"
                                   HorizontalAlignment="Center"
                                   Visibility="{Binding AppuntamentiOggi, Converter={StaticResource GreaterThanConverter}, ConverterParameter=0}"/>

                        <!-- Quick Stats -->
                        <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,20,0,16"/>

                        <TextBlock Text="Statistiche" 
                                   FontSize="14" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimary}" 
                                   Margin="0,0,0,12"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Oggi:" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding AppuntamentiOggi}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Completati:" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding AppuntamentiCompletati}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Totali:" FontSize="12" Foreground="{StaticResource TextSecondary}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Appuntamenti.Count}" FontSize="12" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                        </Grid>

                    </StackPanel>
                </ScrollViewer>
            </Border>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="{StaticResource BorderBrush}"/>

            <!-- ✅ MAIN CALENDAR VIEW - PERFORMANCE OTTIMIZZATO -->
            <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Calendar Headers -->
                    <Border Grid.Row="0" 
                            Background="{StaticResource HoverBrush}" 
                            Height="40" 
                            BorderBrush="{StaticResource BorderBrush}" 
                            BorderThickness="0,0,0,1">
                        <UniformGrid Columns="7">
                            <TextBlock Text="Dom" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Lun" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Mar" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Mer" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Gio" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Ven" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Sab" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13"/>
                        </UniformGrid>
                    </Border>

                    <!-- ✅ CALENDAR GRID VIRTUALIZZATO -->
                    <ScrollViewer Grid.Row="1" 
                                  VerticalScrollBarVisibility="Auto" 
                                  HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding CalendarDays}"
                                      VirtualizingStackPanel.IsVirtualizing="True"
                                      VirtualizingStackPanel.VirtualizationMode="Recycling">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="7" Rows="6"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource CalendarCellStyle}">
                                        <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftDoubleClick" 
                                                          Command="{Binding DataContext.AddAppuntamentoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                        </Border.InputBindings>

                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <!-- Day Number -->
                                            <TextBlock Grid.Row="0" 
                                                       Text="{Binding DayNumber}" 
                                                       FontSize="14" 
                                                       FontWeight="{Binding IsToday, Converter={StaticResource BoolToFontWeightConverter}}" 
                                                       Foreground="{Binding ForegroundColor}" 
                                                       HorizontalAlignment="Left"
                                                       Margin="6,4,0,0"/>

                                            <!-- Events - SEMPLIFICATI -->
                                            <ItemsControl Grid.Row="1" 
                                                          ItemsSource="{Binding Events}"
                                                          ItemTemplate="{StaticResource SimpleEventTemplate}"
                                                          Margin="4,8,4,4">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                            </ItemsControl>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="{StaticResource BorderBrush}"/>

            <!-- ✅ EVENT DETAILS PANEL SEMPLIFICATO -->
            <Border Grid.Column="4" 
                    Background="{StaticResource SurfaceBrush}" 
                    BorderBrush="{StaticResource BorderBrush}" 
                    BorderThickness="1,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="20">
                        <!-- Empty State -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedAppuntamento}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <Ellipse Width="64" Height="64" Fill="{StaticResource BorderBrush}" Margin="0,0,0,16"/>
                            <TextBlock Text="Seleziona un evento" FontSize="16" HorizontalAlignment="Center" 
                                       Foreground="{StaticResource TextSecondary}" FontWeight="SemiBold"/>
                        </StackPanel>

                        <!-- ✅ EVENT DETAILS FORM OTTIMIZZATO -->
                        <StackPanel DataContext="{Binding SelectedAppuntamento}">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>

                            <!-- Event Header -->
                            <Border Background="{Binding StatoColore}" 
                                    CornerRadius="4" 
                                    Padding="12" 
                                    Margin="0,0,0,20">
                                <StackPanel>
                                    <TextBlock Text="EVENTO" 
                                               FontSize="10" 
                                               FontWeight="Bold" 
                                               Foreground="White" 
                                               Opacity="0.8"/>
                                    <TextBlock Text="{Binding Titolo}" 
                                               FontSize="16" 
                                               FontWeight="SemiBold" 
                                               Foreground="White" 
                                               TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding DataInizio, StringFormat='{}{0:dddd, dd MMMM yyyy HH:mm}'}" 
                                               FontSize="12" 
                                               Foreground="White" 
                                               Opacity="0.9" 
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Form Fields - SEMPLIFICATI -->
                            <TextBlock Text="Titolo" Style="{StaticResource OutlookFormLabel}"/>
                            <TextBox Text="{Binding Titolo, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource OutlookFormTextBox}"/>

                            <TextBlock Text="Tipo" Style="{StaticResource OutlookFormLabel}"/>
                            <ComboBox ItemsSource="{Binding DataContext.TipiAppuntamento, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      SelectedItem="{Binding TipoAppuntamento}"
                                      Style="{StaticResource OutlookFormComboBox}"/>

                            <TextBlock Text="Stato" Style="{StaticResource OutlookFormLabel}"/>
                            <ComboBox ItemsSource="{Binding DataContext.StatiAppuntamento, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      SelectedItem="{Binding StatoAppuntamento}"
                                      Style="{StaticResource OutlookFormComboBox}"/>

                            <TextBlock Text="Luogo" Style="{StaticResource OutlookFormLabel}"/>
                            <TextBox Text="{Binding Luogo, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource OutlookFormTextBox}"/>

                            <TextBlock Text="Descrizione" Style="{StaticResource OutlookFormLabel}"/>
                            <TextBox Text="{Binding Descrizione, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource OutlookFormTextBox}"
                                     Height="80" 
                                     TextWrapping="Wrap" 
                                     AcceptsReturn="True"/>

                            <!-- Action Buttons -->
                            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                                <Button Content="💾 Salva" 
                                        Command="{Binding DataContext.SaveAppuntamentoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        Style="{StaticResource PrimaryButton}"/>

                                <Button Content="🗑️ Elimina" 
                                        Command="{Binding DataContext.DeleteAppuntamentoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        Style="{StaticResource BaseToolbarButton}"
                                        Background="{StaticResource ErrorBrush}"
                                        Foreground="White"/>
                            </StackPanel>

                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>